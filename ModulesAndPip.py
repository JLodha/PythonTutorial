import random  #import followed by the py file name that already contains the required functions/variables

print(random.randrange(1,10)) #To access the functions/variables of a module write its name followed by . and then the name of the reqq fn/variable
